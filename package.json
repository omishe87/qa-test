{
  "name": "test",
  "description": "Cypress test qa position",
  "scripts": {
    "test": "cypress run --browser chrome --reporter cypress-teamcity-reporter"
  },
  "dependencies": {
    "cypress": "9.6.0",
    "cypress-cucumber-preprocessor": "4.3.1",
    "cypress-real-events": "^1.7.0",
    "cypress-teamcity-reporter": "^3.0.0",
    "cypress-recurse": "^1.20.0"
  },
  "standart": {
    "globals": [
      "cy",
      "Cypress",
      "beforeEach",
      "expect",
      "Given",
      "When",
      "Then"
    ]
  }
}
